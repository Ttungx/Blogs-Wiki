node_modules/
dist/
.astro/
.env
.DS_Store
*.log
.zcode/
workflow-backup/
.omo/
# 并行 worktree（多 agent 独立工作树）
worktree/

# Wrangler 本地状态（D1 本地副本、临时文件）
.wrangler/

# 临时 SSR 构建检查产物（astro build --outDir）
.tmp-ssr-check/

# batch-translate 临时运行产物（报告/日志/watchdog）
.tmp-bt*/

# 本地 CSS/阅读页视觉回归产物
.css-qa/

# 文章正文与运行状态暂不入库（临时；恢复时删除以下两行）
src/content/articles/
src/data/processed-urls.json

# 语料清理归档（2026-08-24 未同步历史积压，确认可弃后整目录删除）
.corpus-archive/

# 人工训练语料：仅本地训练用，不入库、不进生产构建（该目录从未被 git 跟踪，需入库时删除本行即可）
tran/

# 更新链运行产物（runner 日志 / import payload / 报告）
logs/

# 项目级 opencode 配置（含 Render MCP API key，手动填写后不入库）
opencode.jsonc

# 本地分析草稿与工具状态（不入库；analysis/ 已在 tsconfig exclude）
analysis/
.workbuddy/
.tmp-census.json
