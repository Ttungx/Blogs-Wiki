# Cursor 来源审计（2026-08-10）

## 状态：通过（转 active 受官方中文优先级阻塞）

## 证据

- 发现入口：仅列表页 ok（65 raw / 17 candidates）；RSS `blog/rss.xml` 404（已停更，配置正确省略）；Sitemap `cursor.com/sitemap.xml` 200 但 158 条 URL 中 0 条 `*/zh/blog/*`（且路书记载日期无效，配置正确省略）。
- 三篇样本全部 PASS：
  - `blog/grok-4-5` — 2026-07-08，en，markdown 4135，images 2
  - `blog/agent-swarm-model-economics` — 2026-07-20，en，markdown 25468，images 24
  - `blog/ios-mobile-app` — 2026-06-29，en，markdown 4715，images 1
- 官方中文：`/zh/blog` 200，首页 29563 中文字符；`/zh/blog/grok-4-5` 为完整中文译文（title=推出 Grok 4.5 · Cursor，29501 中文字符）。中文质量已核验为真实全译。
- 阻塞点：英文文章页**无 hreflang alternate**，`prefer_official_zh` 走 hreflang 检测不会命中；Sitemap 不含 zh 路由，无法经 sitemap 发现中文版。当前管线只能发现 en 版。

## 建议的 sources.json 配置修正

- 保持无 rss / 无 sitemap、`article_paths: ["/blog"]`、`exclude_paths: ["/blog/topic"]`（列表页已含 17 篇，够用）。
- `prefer_official_zh: true` 可先写入表达意图，但当前无效；真正生效需管线支持"en URL → /zh/<slug> 探测"或按 slug 合并 zh 列表页，且需去重避免 en/zh 双收。
- 若在管线增强前激活：接受 en 版走模型翻译，zh 版暂不收录；这偏离路书"官方中文优先"，不建议。

## 可否转 active

暂不可。发现/抓取本身通过，但官方中文存在且质量好，缺失 hreflang/zh 路由探测能力会导致中文重复翻译。建议先落地 zh 路径探测（fetch 层），再转 active。
