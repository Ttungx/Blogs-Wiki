# Qwen 来源审计（2026-08-10）

## 状态：通过（官方中文存在，但管线无法命中，转 active 受阻）

## 证据

- 发现入口：Sitemap `en/sitemap.xml` ok（53 raw / 44 candidates）；列表页 0 candidates（qwenlm.github.io 首页无 blog 列表）；无 RSS（配置正确省略）。
- 三篇样本全部 PASS：
  - `blog/qwen3guard` — 2025-09-22，en，markdown 14087，images 4
  - `blog/qwen-image-edit` — 2025-08-18，en，markdown 8634，images 22
  - `blog/qwen-image` — 2025-08-04，en，markdown 10876，images 13
- 官方中文（重点核验）：`zh/sitemap.xml` 200，47 条 URL 中 45 条 blog（`/zh/blog/qwen3guard/` 等）；`/zh/blog/qwen3guard` 200，9469 中文字符，为完整官方简体中文译文。中文版本真实存在且覆盖面广。
- 阻塞点：英文文章页**无 hreflang alternate**，`prefer_official_zh`（hreflang 检测）不会命中；当前 sitemap 只配置 en，zh 版本完全不可达。若改配 zh sitemap 则丢失 en 覆盖，且无 en/zh 去重逻辑，会双收。

## 建议的 sources.json 配置修正

- 维持 `sitemap_url: en/sitemap.xml`、`article_paths: ["/blog"]`、dry-run-only。
- `prefer_official_zh: true` 可先写入表达意图，但当前无效；生效前提是管线支持 zh 路径探测（en URL → `/zh/<slug>`）或 zh sitemap 合并 + 按 slug 去重。
- 长期建议：zh sitemap（45 篇）作为官方中文索引纳入发现，仅当 en/zh 去重落地后。

## 可否转 active

暂不可。官方中文比例高（45/44 篇有 zh 版），在无 zh 命中能力下转 active 会全量模型翻译，浪费且违背路书"官方中文优先，不重复翻译"。需先落地 zh 探测/去重，再转 active。
